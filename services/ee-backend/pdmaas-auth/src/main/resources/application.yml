server:
  port: 8081

spring:
  application:
    name: pdmaas-auth

  datasource:
<<<<<<< HEAD
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ddm_ee}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:ddm123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: DDM-Auth-Pool
      maximum-pool-size: 10
      minimum-idle: 2
=======
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ddm_ee}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:ddm123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
>>>>>>> 054b79eefc13aee90d6bb8102ac519f653988797

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      database: 2
<<<<<<< HEAD
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  security:
    # 禁用 Spring Security 默认的 basic auth（我们用 JWT）
    user:
      name: ddm-admin
      password: ddm-secret

# JWT 配置
jwt:
  secret: ${JWT_SECRET:ddm-ee-super-secret-key-change-in-production-min-32chars}
  expiration: 86400         # Access Token 有效期（秒），默认 24h
  refresh-expiration: 604800 # Refresh Token 有效期（秒），默认 7天

# 日志
logging:
  level:
    com.datadictmanage: DEBUG
    org.springframework.security: INFO
=======

jwt:
  secret: ${JWT_SECRET:ddm-ee-super-secret-key-change-in-production}
  expiration: 86400

logging:
  level:
    com.datadictmanage: DEBUG
>>>>>>> 054b79eefc13aee90d6bb8102ac519f653988797
